<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practiceactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeActivity">
        <activity
            android:name=".SelectServiceActivity" android:label="Select Service" android:parentActivityName=".MainActivity"/>
<!--            android:exported="true" />-->
        <activity
            android:name=".CreateListActivity" android:label="New List" android:parentActivityName=".MainActivity"/>
<!--            android:exported="true" />-->
        <activity
            android:name=".ViewListActivity" android:label="View Lists" android:parentActivityName=".SelectServiceActivity"/>
<!--            android:exported="true" />-->
        <activity
            android:name=".MainActivity" android:label="Home">
<!--            android:exported="true">-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>